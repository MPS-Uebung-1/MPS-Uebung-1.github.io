// This is a javaScript file

var a = "Hello ";

var b = "!";

function hello(name) {
    return a + name + b;
};

var button = document.getElementById("firstButton")

var buttonList = document.getElementsByClassName("myButton")

console.log(hello("world"));